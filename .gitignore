# Live store (LMDB directory). The committable artifact is `memgraph dump` output.
.memgraph/
*.dump.jsonl
bench/results/

# Book build artifacts (sources live in book/chapters/; `bb book` regenerates these)
book/rendered/
.clay/
.cpcache/
